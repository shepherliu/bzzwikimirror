// example imports 
import * as constant from '../constant'

import { ref } from "vue"

export const connectState = {
  activeIndex: ref('1'),
  activeName: ref(''),
  searchCallback: async () => {},
};
